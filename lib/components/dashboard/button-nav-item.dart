import 'package:flutter/material.dart';

class ButtonNavItem extends StatelessWidget {
  const ButtonNavItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
